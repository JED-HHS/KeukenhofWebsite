@model IEnumerable<KeukenhofWebsite.Models.Pagina>

@{
    ViewData["Title"] = "Index";
    Layout = "_AdminLayout";
}

<h2>Index</h2>

<div>
    <a asp-action="GoToMainIndex">Go back to main index</a>
</div>

<p>
    <a asp-action="Create">Create New Pagina</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaginaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PaginaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PaginaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
